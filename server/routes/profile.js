// Express route for user
const express = require('express');

// define router
const userRouter = express.Router();

// defining the GET endpoint, which displays user's profile
profileRouter.get('/profile/:id', async () => {
    
});